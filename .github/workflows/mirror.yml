name: Mirror to SourceForge

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  call-mirror:
    uses: kylofon/ci-workflows/.github/workflows/sourceforge-mirror.yml@master
    with:
      sf_project: test-drive-2-sdl3
    secrets:
      SF_SSH_KEY: ${{ secrets.SF_SSH_KEY }}
